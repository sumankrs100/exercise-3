version: '3'

services:
  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A server1.tasks worker -Q server1_queue,server2_queue --loglevel=info
    environment:
      - REDIS_HOST=server2_ip_address  # Replace with actual IP of server 2
      - REDIS_PORT=6379
    volumes:
      - ./server1:/app/server1
    restart: unless-stopped

  celery_beat:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A server1.tasks beat --loglevel=info
    environment:
      - REDIS_HOST=server2_ip_address  # Replace with actual IP of server 2
      - REDIS_PORT=6379
    volumes:
      - ./server1:/app/server1
    restart: unless-stopped
